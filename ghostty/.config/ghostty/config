command = fish --login --interactive

shell-integration = fish

theme = 0x96f

font-family  = "TX-02 Regular"
font-size    = 12
font-feature = -calt, -liga, -dlig
font-thicken = true

# Quick Terminal Settings
quick-terminal-position = top
quick-terminal-animation-duration = 0.1
quick-terminal-size = 30%,90%
quick-terminal-space-behavior = move
keybind = global:ctrl+`=toggle_quick_terminal

clipboard-trim-trailing-spaces = true

keybind = ctrl+b>h=goto_split:left
keybind = ctrl+b>j=goto_split:bottom
keybind = ctrl+b>k=goto_split:top
keybind = ctrl+b>l=goto_split:right

keybind = ctrl+b>shift+h=new_split:left
keybind = ctrl+b>shift+j=new_split:down
keybind = ctrl+b>shift+k=new_split:up
keybind = ctrl+b>shift+l=new_split:right
keybind = ctrl+b>shift+f=toggle_split_zoom

keybind = ctrl+b>n=next_tab
keybind = ctrl+b>p=previous_tab

# keybind = super+r=reload_config

# keybind = super+shift+p=toggle_command_palette

# https://catskull.net/fun-with-ghostty-shaders.html
# custom-shader = ./shaders/cursor_blaze.glsl

# Include other configuration files
# Processing Order: The config-file keys are processed at the very END of the file.
# This means that even if you place config-file at the top of the main file,
# any overlapping settings inside the included files will overwrite the settings
# in the main config file.
config-file = ?keybinds.conf
config-file = ?macos-specific.conf
config-file = ?nix.conf
